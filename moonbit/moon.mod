name = "fanfeilong/filefs"

version = "0.1.0"

readme = "README.mbt.md"

repository = "https://github.com/fanfeilong/FileFS"

license = "MIT"

keywords = [ "filesystem", "filefs", "vfs" ]

preferred_target = "wasm-gc"

description = "MoonBit port of FileFS (virtual filesystem in a single file)"

import {
  "moonbitlang/x@0.4.47",
}
